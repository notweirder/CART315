using UnityEngine;
using Pathfinding;

public class courseUpdater : MonoBehaviour
{
      
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        //AstarPath.active.Scan();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
